#ifndef COMMANDS
#define COMMANDS

enum progCommands {
    COMMAND_PUSH = 1,
    COMMAND_POP = 2,

    COMMAND_ADD = 3,
    COMMAND_SUB = 4,
    COMMAND_MUL = 5,
    COMMAND_DIV = 6,

    COMMAND_OUT = 7,
    COMMAND_IN = 8,
    COMMAND_HLT = 0,
};

#endif